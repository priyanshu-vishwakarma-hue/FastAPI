python3 -m venv myenv    ->   to create the virtual environment

source myenv/bin/activate     ->    to antivate the myenv
deactivate    ->     to deactivate myenv


pip install -r requirement.txt

pip list   -> gives all the package and there version



uvicorn main:app  -> to run

uvicorn main:app --reload    ->   to run but it automatically reload the server on changing the code 



